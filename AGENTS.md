# Rescue Town — 구현 에이전트 작업 규칙

## 시작점
먼저 `docs/49_SPEC_AUTHORITY_AND_HANDOFF.md`, `docs/47_ASTRA_IMPLEMENTATION_WORKBOOK.md`, `data/implementation_plan.json`을 읽는다. 이 파일은 저장소의 작업 지침이며 상위 안전·권한 지침을 대체하지 않는다.

목표는 **시골마을 1개, 고양이 50종/50마리, 건물 13종, 실제 모바일 광고를 포함한 완결된 v1.0**이다. 개발 중 한 마리 관통 시험은 내부 단계이며 축소판 출시 허가가 아니다.

## 바꾸지 않는 핵심
- 고양이에게 희귀도/캐릭터 레벨/숨은 성장등급을 만들지 않는다. 고유 기본능력 + 실제 근무 경험으로 성장한다.
- 네 발 고양이, 고유 Resident ID, 전 화면 동일 외형. 가짜 주민으로 빈 마을을 채우지 않는다.
- 정착+진행 구조 정원은 최대50. 건물별 직원 최대3. 정식 근무지 한 곳, 재배치 무료.
- 주민 정원은 구조센터, 건물 레벨은 min(자체 최대, 센터 레벨). 주택은 이중 구조 제한이 아니다.
- 생활 연출/프레임/현재 카메라와 경제·경험 계산을 분리한다.
- 첫8마리는 onboarding, 나머지는 discovery_rules 조건식. primaryCondition 문자열을 임의 해석하지 않는다.
- 광고 없이 모든 핵심 콘텐츠 접근. 기본 복귀보상과 광고추가를 분리한다.
- 새 지역, 프레스티지, IAP, PvP, 길드, 원격 밸런스, 실시간 LLM 주민 AI를 추가하지 않는다.

## 구현 방식
선정 엔진은 Unity 6.3 LTS 계열이다. `game/RescueTown/` 아래에 프로젝트를 만든다. 정확한 패치와 의존성 잠금은 M0에서 실제 설치·빌드 후 기록한다. 엔진을 임의 교체하지 않는다.
`data/`는 기획 원본이다. 게임에 포함하는 복사본은 빌드 과정에서 생성하고 체크섬으로 대조한다. 보고서·시뮬레이션 설정을 게임 데이터에 섞지 않는다.
기존 기획·검사기를 삭제하거나 임시 구현에 맞춰 테스트를 약화하지 않는다. 핵심 로직은 UnityEngine/광고SDK/DB를 참조하지 않는 C# 계층에 둔다.

## 변경과 실행
1. 현재 브랜치, HEAD, 미커밋 변경, 프로젝트별 에디터 실행을 확인한다.
2. 기능 브랜치 `impl/m0-foundation` 등에서 작은 검증 가능한 단위로 작업한다. 사용자 변경은 보존한다.
3. 같은 Unity 프로젝트/Library를 두 에디터나 에이전트가 동시에 쓰지 않는다. 병렬 작업은 별도 worktree와 작업 소유권을 사용한다.
4. Scene/Prefab/설정은 재실행 가능한 Editor 생성·검증 코드로 연결한다. 빈 게임 오브젝트나 단일 거대 MonoBehaviour에 모든 기능을 몰아넣지 않는다.
5. 수정 → 실제 컴파일/테스트 → 로그 확인 → 상태 갱신 → 커밋한다. 스텁/가짜광고/스크린샷만으로 완료 선언하지 않는다.
6. 실행하지 못한 검사는 BLOCKED 또는 NOT_RUN. 과거 Python 보고서를 새 Unity 통과 증거로 복사하지 않는다.

## 외부 권한
계정 로그인, 라이선스 동의, 인증서/서명, 실제 광고단위·콘솔 설정, 유료 구매, 공개 서버 배포, 스토어 제출은 권한 경계다. 필요한 항목만 `docs/50_IMPLEMENTATION_STATUS.md`에 기록하고 승인되지 않은 외부 변경은 하지 않는다. 독립적으로 가능한 개발은 계속한다.
비밀키/토큰/인증서/keystore/실사용 저장/자유입력 주민이름을 공개 Git에 올리지 않는다. 앱·광고 단위 ID와 비밀키를 혼동하지 않는다.
실광고를 반복 재생·클릭하여 테스트하지 않는다. 공식 테스트 광고와 테스트기기를 사용한다.

## 산출물과 종료 보고
각 단계 보고에는 커밋, 실제 명령, 종료코드, 실패/스킵 수, 증거 경로, 기기/SDK 버전, 남은 차단항목을 적는다. 화면 증거는 실제 런타임에서 촬영한다.
상태는 `not_started / in_progress / blocked_external / failed / passed` 중 하나다. 사용자에게 매번 컨셉 재승인을 요구하지 말고 작업서의 기본값으로 진행한다. 범위 변경·유료작업·실제 아트 방향 승인은 별도로 받는다.
첫 사용자 구현 지시는 `prompts/ASTRA_START.md`를 사용한다.
